<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PPTcliente"
        tools:targetApi="31">
        <activity
            android:name=".jogo.ServerGameActivity"
            android:exported="false" />
        <activity
            android:name=".jogo.ClienteGameActivity"
            android:exported="false" />
        <activity
            android:name=".serverConfig.ClienteActivity"
            android:exported="false" />
        <service
            android:name=".services.MyServerService"
            android:exported="true" />
        <service
            android:name=".services.MyClienteService"
            android:exported="true"  />

        <activity
            android:name=".serverConfig.ServerActivity"
            android:exported="false"
            android:theme="@style/Theme.PPTcliente" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PPTcliente">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>